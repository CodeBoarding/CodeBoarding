apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deploy
spec:
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
        - name: api
          image: acme/api:1
