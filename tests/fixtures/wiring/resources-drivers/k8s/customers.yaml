apiVersion: apps/v1
kind: Deployment
metadata:
  name: customers-service
spec:
  template:
    metadata:
      labels:
        app: customers
    spec:
      containers:
        - name: customers
          image: acme/customers-service:1
