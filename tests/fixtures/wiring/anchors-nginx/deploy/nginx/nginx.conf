include /etc/nginx/site-include/upstreams;

server {
    location /json {
        proxy_pass http://django;
    }
    location /notify {
        uwsgi_pass tornado;
    }
    location /api {
        proxy_pass http://api;
    }
    location /any {
        proxy_pass http://$backend;
    }
}
